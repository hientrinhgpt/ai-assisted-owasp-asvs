# ai-assisted-owasp-asvs
This MCP server is designed for **trusted local environments** where the MCP client (e.g., Github Copilot/Claude Code) has already authenticated the user. The server provides read-only access to OWASP ASVS security requirements data.
